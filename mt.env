# Configuration template (.env)
# Copy this file and rename it to ".env" to use it

# Your backend URL.
# Locally, it is usually http://localhost:4000
VITE_BACKEND_URL=http://localhost:4000

# Enable application-level development features (debug displays, proxy, etc.)
# Note: this does NOT control Vite's mode or import.meta.env.DEV; those are set by Vite itself.
DEV=true
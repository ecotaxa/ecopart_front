services:
  web:
    image: 'ecotaxa/ecopart_front:latest'
    # No env_file: VITE_* variables are baked in at build time, the runtime
    # container only serves static files.
    ports:
      - "3000:3000"
    restart: unless-stopped
